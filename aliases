# Unix
alias ll="ls -alh"
alias v="nvim"
alias vim="nvim"

# Reload ZSH
alias z="source $HOME/.zshrc"

# Vim
alias vimdiff="vim -d"

# Misc
alias pdf="evince"
alias dots="cd $HOME/.dotfiles"
alias launch="cd $LAUNCH"
alias cl="cd $WORK"
alias notes="cd $NOTES"
